# Android native speaking-audio — deferred

Placeholder for the Capacitor Android implementation of `@vni/speaking-audio`.

Required before this folder holds code:

- AudioManager focus handling with `INTERRUPTED` vs `PAUSED`
- Background-capable recording
- Persist to app storage before upload (`audio/webm`)

See [ADR-0006](../../../docs/decisions/0006-speaking-audio-capture-native-plugin.md) and the package [README](../README.md).
