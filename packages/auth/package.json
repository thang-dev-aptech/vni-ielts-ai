{
  "name": "@vni/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "HTTP transport and the session core, shared by the learner app and the CMS",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@vni/api-client": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@vni/config": "workspace:*",
    "jsdom": "27.0.0",
    "typescript": "5.9.2",
    "vite": "8.3.0",
    "vitest": "3.2.7"
  }
}
