{
  "run": "mvp-code-first-2026-09-07",
  "plan": "_workspace/design-brief/claude-code-handoff.md (S1..S9), decisions P-01..P-22 in docs/product/mvp-blueprint.md",
  "createdAt": "2026-09-07T14:45:00+07:00",
  "updatedAt": "2026-09-07T19:10:00+07:00",
  "orchestrator": "claude (main session)",
  "note": "Wave 1's five agents (T1-T5) all hit the session's monthly spend limit mid-task and stopped. The orchestrator inspected each one's partial work directly, fixed compile errors and finished the missing pieces (Infrastructure persistence, DI, config, endpoints, docs, tests) by hand rather than re-dispatching, since the limit's reset time was uncertain. All five are now verified complete with red-when-removed evidence captured and restored.",
  "sharedOwnership": {
    "contracts/openapi/v1.json + packages/api-client": "orchestrator only — regenerate once per backend wave via OpenApiContractTests, deferred to T15",
    "CLAUDE.md, docs/README.md": "orchestrator only (S9)",
    "_workspace/workflow/task-board.json": "orchestrator only"
  },
  "tasks": [
    { "id": "T1", "slice": "S4", "title": "Writing task weights 1:2 as configured seam; combined Writing band via BandScore.Weighted", "owner": "backend-engineer+orchestrator", "status": "done", "dependsOn": [], "wave": 1,
      "evidence": "WritingTaskWeightPolicyTests, WritingBandResultsTests, WritingTaskWeightOptionsTests (22 tests) all green. Fixed a pre-existing-file contamination bug the slice exposed: StartupConfigurationTests.A_half_stated_writing_task_weighting_is_rejected silently inherited Task1=1 from the Api project's appsettings.json (copied into the test project's own output by the project reference) because the test only overrode Task2 — fixed by explicitly nulling Task1 in the in-memory config override." },
    { "id": "T2", "slice": "S3", "title": "Append-only usage ledger, 10-turn grant, daily login + referral earn, GET /api/v1/me/usage", "owner": "backend-engineer+orchestrator", "status": "done", "dependsOn": [], "wave": 1,
      "evidence": "Domain (UsageEntry + 9 tests), Application (UsageRecorder/GetMyUsage + 21 tests incl. 'never blocks' proof), Infrastructure (MongoUsageLedger, MongoReferralDirectory, usage_ledger collection+index), DI, config seam (Usage:InitialGrantTurns=10; DailyLoginTurns/ReferralTurns deliberately left at 0 — G-11, no owner-given amount), GET /api/v1/me/usage endpoint, migration-plan.md row. Wired grant+referral at all 4 remaining touchpoints the crashed agent left unwired: RegisterUser (grant + referral attribution), SignInWithSso (grant on first social login only), ConfirmEmailCode + VerifyEmail (referral earn on verification, guarded against double-count), LearnerPresence (daily earn). Fixed the referral-code/referredByUserId persistence gap: UserDocument/IdentityMappers never carried those two fields, so they'd silently vanish on the first save-reload — added the Mongo fields + a sparse unique index. Red-when-removed captured for RegisterUser's grant+referral wiring (reverting it doesn't even compile — CS9113 unused-parameter) and for UsageRecorder's never-blocks guarantee." },
    { "id": "T3", "slice": "S5", "title": "Documents and Articles: collections, CRUD + publish API, learner read API", "owner": "backend-engineer+orchestrator", "status": "done", "dependsOn": [], "wave": 1,
      "evidence": "Domain/Application/Infrastructure/DI/endpoints/permission-grants were already complete when the agent crashed. Orchestrator added the missing test suite (18 tests: learner published-only filter, draft-slug-is-404, slug uniqueness incl. draft-reserves-slug, relatedExamIds null round-trip, publish-from-draft with no separate approved state, delete-blocked-until-unpublished) and the migration-plan.md + cms-content-operations.md doc notes. Red-when-removed captured for Article slug uniqueness by disabling both enforcement layers (handler pre-check AND store) at once. Finding: CreateLibraryDocument/UpdateLibraryDocument (unlike CreateArticle/UpdateArticle) have no slug-uniqueness handling at all — left as-is since nothing routes a document by slug server-side (GET is by id), but flagged for whoever revisits this." },
    { "id": "T4", "slice": "S6a", "title": "ZIP safety inspector + skill-folder layout + sandbox extraction (no endpoint)", "owner": "security-engineer+orchestrator", "status": "done", "dependsOn": [], "wave": 1,
      "evidence": "ExamPackageArchiveInspector + ImportArchiveOptions (configured seam in appsettings.json) + HostileArchives test fixtures, 50 tests green. Orchestrator fixed 4 bugs the crashed agent left: (1) total-uncompressed-size check ran after per-entry checks so a bomb's ratio failure masked the total-size finding — moved it before the per-entry loop, straight off the central directory; (2) directory entries with non-zero declared length were wrongly flagged ENTRY_NOT_REGULAR because the test fixture's default content ('x') doesn't pair with Build()'s 'skip content for empty dir' rule — fixed the fixture, not the (correct) production check; (3) unknown-entry ordering used OrdinalIgnoreCase which folds '_' after letters — switched to Ordinal; (4) the sandbox directory survived a quota-exceeded failure because Cleanup() ran while the FileStream that had just written to it was still open inside the same using block — restructured to capture the finding and return only after the stream's own dispose runs." },
    { "id": "T5", "slice": "S1b+S8", "title": "Web: timingFor no default, FullTestReadinessModal from catalogue, remove EntryTestModal, Vietnamese ErrorBoundary, coming-soon strings", "owner": "frontend-engineer", "status": "done", "dependsOn": [], "wave": 1,
      "evidence": "Completed cleanly by the agent before it hit the limit; all 32 web test files / 312 tests pass including the new practice-dead-ends.test.tsx. No orchestrator fixes needed." },
    { "id": "T6", "slice": "S1a+S2", "title": "Backend: bandTableProvenance → results bandVerified, provenance on results/markings, post-submit exam content, recording playback GET", "owner": "backend-engineer+orchestrator", "status": "done", "dependsOn": ["T1"], "wave": 2,
      "evidence": "ScoringProfile.Provenance (BandTableProvenanceStatus Synthetic|Provisional|Equated) read by ExamPackageReader, round-tripped through Mongo. SectionResultView.BandVerified = version.Scoring.Provenance?.Status == Equated, computed alongside Band (never replacing it). SessionResultsView.Content: SectionContentView[] (reuses PartView exactly, no transcript, no answer key), gated strictly on session.Status != InProgress, built via a new IAnswerSheetStore.LoadWritingSubmissionsAsync that reuses SectionMarkingRunner's exact read path (the one that fixed the 2026-09-03 slot-id defect) rather than a second one. Recording playback: ISpeakingRecordingBlobStore.CreatePresignedGetUrl + GET /api/v1/sessions/{id}/recordings/{questionId}/playback, 404 via the same LoadOwnedAsync ownership pattern every sitting route uses. 19 new tests across Domain/Infrastructure/Application. Orchestrator verified directly: full solution build clean; Domain 223/223, Application 398/399 (1 pre-existing unrelated), Infrastructure 287/290 (3 pre-existing unrelated skips), Architecture 10/10, Integration 223/224 (only the expected OpenAPI drift). Spot-read BandVerified computation, the InProgress content gate, and the playback ownership check directly — all correctly gated, well-reasoned." },
    { "id": "T7", "slice": "S7", "title": "Backend: InReview/Approved, exam.review permission, 4 audit actions, reviewer != author enforced", "owner": "backend-engineer+orchestrator", "status": "done", "dependsOn": ["T3"], "wave": 2,
      "evidence": "ExamVersionStatus gains InReview, Approved. ExamVersion gains AuthorId (UserId?, nullable — import pipeline doesn't thread an actor yet, flagged as [OPEN QUESTION] rather than guessed), SubmitForReview/Approve/ReturnToDraft(reason). Approve throws ReviewerIsAuthorException when AuthorId == reviewerId, enforced at the domain level so it holds regardless of caller permissions. PermissionKeys.ExamSubmit/ExamReview (admin gets both, content-editor gets submit only). AuditAction gains ExamSubmittedForReview/ExamApproved/ExamReturnedToDraft/WarningOverridden (the last reserved for S6b). Three new endpoints in AdminEndpoints.cs; PublishEndpoint now requires Approved or previously-Published. Necessary, narrowly-scoped fix to a shared test (ContentRightsPublishTests: one success-path fixture repointed from Draft to Approved since the new precondition is orthogonal to what that test checks; the three refusal-path tests untouched). Orchestrator verified: build clean, Domain 223/223 incl. 11 new tests, Application 398/399, Architecture 10/10. Read the domain methods and the test-fixture diff directly — correctly scoped, clearly documented open question on AuthorId." },
    { "id": "T8", "slice": "S6b", "title": "Backend: ZIP upload endpoint, batch progress, draft/batch persistence, blocking/warning split, warning override with reason + audit", "owner": "backend-engineer+orchestrator", "status": "done", "dependsOn": ["T4", "T7"], "wave": 3,
      "evidence": "AdminImportEndpointsTests (4 endpoints: upload, get-draft, override-warning-with-reason, approve). MongoImportDraftStore (CAS via Revision-filtered ReplaceOneAsync, insert dedup on the content-derived StableDraftId), MongoImportBatchCheckpointStore. FabricatedAnswerKeyGuard now wired into ImportExtractedAsync (was never called in production before this slice — a real pre-existing gap this closes). Honest, well-scoped decision: no AI provider wired into the hosted API's IExamSourceParser (UnconfiguredExamSourceParser → typed 422), so only packages with a ready exam.json import over HTTP today; batch HTTP endpoint not built (checkpoint store is, the surface isn't) — both explicitly flagged rather than half-built. Orchestrator verified: build clean, Domain 223/223, Application 401/402 (1 pre-existing unrelated), Architecture 10/10, Infrastructure 296/299 (3 pre-existing unrelated skips), Integration AdminImportEndpointsTests 5/5, check-docs green (23 collections)." },
    { "id": "T11", "slice": "S5", "title": "Admin CMS: Documents and Articles screens on real API", "owner": "frontend-engineer+orchestrator", "status": "done", "dependsOn": ["T3"], "wave": 2,
      "evidence": "DocumentsPage.tsx/ArticlesPage.tsx, libraryLifecycle.ts (deliberately not extending the exam-review lifecycle.ts — different shape, different task), LibraryStatusBadge.tsx, 12 new tests. Sidebar entries un-inerted, routes gated any-of document.write|document.publish (matching the server's DeniedUnlessAny). Orchestrator verified: 77/77 admin tests pass, typecheck clean, spot-read libraryLifecycle.ts — correct and well-documented." },
    { "id": "T12", "slice": "S5", "title": "Learner web: /documents and /articles read from API; delete hard-coded arrays", "owner": "frontend-engineer", "status": "done", "dependsOn": ["T3"], "wave": 2,
      "evidence": "documentsApi.ts/articlesApi.ts, components rewired to fetch (loading/failed/empty states), ArticleSummary/Article type split matching backend's view split. Found and fixed a real crash: a malformed/missing-items API response threw inside LandingPage's render — added defensive `items ?? []`. Orchestrator verified: 311/311 web tests pass." },
    { "id": "T13", "slice": "S1a", "title": "Web results: bandCell from bandVerified, advisory label from provenance via @vni/types.requiresAdvisoryLabel, post-submit content, combined Writing band, recording playback", "owner": "frontend-engineer+orchestrator", "status": "done", "dependsOn": ["T6", "T5"], "wave": 3,
      "evidence": "bandCell gated on bandVerified (P-11). Advisory label improved beyond the brief: provenance derived from which array a row's data came from (sections=answer-key vs markings=ai-advisory) rather than a moduleId string check, so a hypothetical fifth AI-marked module labels correctly. Combined Writing band card + Vietnamese reason text for both null-reasons. Post-submit left-column content reusing PassageBody/ExamImage. Recording playback via a native <audio> (AudioPlayer.tsx doesn't accept a src prop, is on the D-11 preserve list, correctly left untouched). Orchestrator verified: 318/318 web tests pass, full repo-wide typecheck clean across all 10 workspace projects (contractParity.test.ts included)." },
    { "id": "T14", "slice": "S1e+S7+S6+S8", "title": "Admin: permissions from GET /admin/roles, exam review-lifecycle UI reconciled with the 5-state server model (draft/in-review/approved/published/unpublished, no separate returned), import UI wired to T8's endpoint, sidebar cleanup", "owner": "frontend-engineer+orchestrator", "status": "done", "dependsOn": ["T7", "T8", "T11"], "wave": 4,
      "evidence": "Verified after mid-session interrupt. Fixed: exam-review-lifecycle findByText→findByRole/getAllByRole (title+confirm button duplicated); import-upload mock return for overrideImportWarning (undefined draft crashed findings). 87/87 admin tests + typecheck green. Red-when-removed: RolesPage iterating Object.keys(PERMISSION) instead of API permissions[] → roles-matrix 2 failures (frobnicate missing + media.upload leaked); restored. Requirements: (a) RolesPage columns from GET /admin/roles permissions[] (34 keys in PermissionKeys.All); (b) lifecycle 5 states draft|inreview|approved|published|unpublished, no returned; (c) ImportPage→POST /admin/import/packages multipart file. ReviewQueue/PendingPublish on listExams; MediaLibrary deliberately remains on previewStore (no media.* keys/endpoints) with PreviewNotice. MyExams/WorkflowDetail deleted; App/AdminShell/paths have no dead routes." },
    { "id": "T15", "slice": "all", "title": "Contract regeneration + api-client + typecheck + full test suites + e2e", "owner": "orchestrator+qa-engineer", "status": "done", "dependsOn": ["T13", "T14", "T12"], "wave": 5,
      "evidence": "OpenApiContractTests pass1 fail→wrote contracts/openapi/v1.json; pass2 3/3. pnpm typecheck regenerates api-client, 10 projects green. Backend Domain 223, Application 401/402 (1 pre-existing AnswerKeyTests), Architecture 10, Infrastructure 296/299 (3 skip), Integration 229, Worker 13. Admin 87, web 318. E2E: Chromium installed; stage-synthetic.mjs before API boot; seeder re-publishes orphan-unpublished synthetic; four-skills clickNext confirms AdvanceConfirmCard. Full e2e 20/34 (desktop smoke/practice/races/resilience green). Remaining: four-skills Writing Part 2 nav + mobile locators — recorded, not loosened." },
    { "id": "T16", "slice": "S9", "title": "Docs after slices, ADRs, CLAUDE.md inventory, open questions, check-docs", "owner": "orchestrator", "status": "done", "dependsOn": ["T15"], "wave": 5,
      "evidence": "CLAUDE.md inventory rewritten from source. B-14 AuthorId nullable kept [OPEN QUESTION]. ADR-0017. docs/README ADR table through 0017. check-docs green." },
    { "id": "T17", "slice": "config", "title": "Recording retention days + sweep enabled as configuration", "owner": "orchestrator", "status": "done", "dependsOn": [], "wave": 5,
      "evidence": "appsettings.json: ObjectStorage:SpeakingRecordingRetentionDays=90, Recordings:SweepEnabled=true. API boot log confirms retention=90." }
  ],
  "environment": {
    "docker": "running; Mongo 27018 / MinIO 9000 / pbm healthy",
    "node": "v22.22.2 (package.json asks >=24; vitest runs fine)",
    "dotnet": "10.0.301. Post T15: Domain 223/223, Application 401/402 (1 pre-existing), Architecture 10/10, Infrastructure 296/299 (3 skip), Integration 229/229, Worker 13/13. E2E 20/34 after synthetic staging fix."
  }
}
